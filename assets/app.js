 : view === 'insights' ? document.body.dataset.insightsSubtitle : document.body.dataset.librarySubtitle;    document.title = (savedOnly ? 'Saved Articles' : view === 'insights' ? 'General Articles' : 'Research Library') + ' | The Private Ledger';    document.querySelectorAll('[data-view]').forEach(a => { a.classList.toggle('active',a.dataset.view === view); if (a.dataset.view === view) a.setAttribute('aria-current','page'); else a.removeAttribute('aria-current'); });    chips.forEach(c => c.setAttribute('aria-pressed',String(c.dataset.sector === sector)));    savedButton.setAttribute('aria-pressed',String(savedOnly));  }  search.addEventListener('input',() => { render(); syncUrl(); });  sort.addEventListener('change',() => { render(); syncUrl(); });  chips.forEach(c => c.addEventListener('click',() => { sector = c.dataset.sector; render(); syncUrl(); }));  savedButton.addEventListener('click',() => { savedOnly = !savedOnly; sector = 'All sectors'; render(); syncUrl(); });  document.querySelectorAll('[data-view="library"],[data-view="insights"]').forEach(a => a.addEventListener('click',event => {    if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;    event.preventDefault(); view = a.dataset.view; sector = 'All sectors'; savedOnly = false; search.value = ''; syncUrl(true); render();  }));  window.addEventListener('popstate',() => { view=currentView(); sector=params().get('sector') || 'All sectors'; search.value=params().get('q') || ''; savedOnly=params().get('saved') === '1'; sort.value=params().get('sort') || 'new'; render(); });  document.querySelector('#reset').addEventListener('click',() => { sector='All sectors';search.value='';savedOnly=false;render();syncUrl();search.focus(); });  document.addEventListener('keydown',event => { if (event.key === '/' && !event.ctrlKey && !event.metaKey && !event.altKey && !document.activeElement.isContentEditable && !['INPUT','TEXTAREA','SELECT'].includes(document.activeElement.tagName)) {event.preventDefault();search.focus();} });  document.addEventListener('ledger:state',render);  render();})();
